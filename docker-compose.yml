services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    image: vh-ipfs
    ports:
      - "16661:16661"
      - "16662:16662"
    volumes:
      - ./data:/data
      - ./ipfs-data:/data/ipfs
